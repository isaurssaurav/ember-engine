/* eslint-env node */
'use strict';

module.exports = function(environment /*, appConfig */) {
  const ENV = {
    modulePrefix: 'ember-engine-one',
    environment: environment
  }

  return ENV;
};
